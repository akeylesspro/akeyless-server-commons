# src/types/interfaces/global.ts

## Purpose

Interfaces for request validation and app initialization options.

## Exports

- `MandatoryObject`: validation rule for a single parameter.
- `MandatoryParams`: optional lists of body/header validation rules.
- `LogRequests`: flags for request logging sections.
- `AppOptions`: options for server startup and snapshot/Redis init.
