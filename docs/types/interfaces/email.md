# src/types/interfaces/email.ts

## Purpose

Email data models for SendGrid helpers.

## Exports

- `EmailData`: input payload for `send_email`.
- `EmailAttachment`: base64 attachment model.
- `EmailSettings`: Firestore settings model for email groups and defaults.
