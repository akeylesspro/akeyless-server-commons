# src/types

## Contents

- `types/`: type aliases for helpers, snapshots, and core API shapes.
- `interfaces/`: interfaces for middleware options and email models.
- `enums/`: shared enums and service map types.
- `index.ts`: barrel export for all types.

## Context

Shared types keep helpers, managers, and middlewares consistent.
